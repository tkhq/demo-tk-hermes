Turnkey organization setup through the tk CLI: tags, agent users, export policies
with human approval, and secret imports. This profile gives the agent no shell, so
the agent explains the exact commands and the operator runs them in a trusted
terminal. Secret values never enter the chat.
